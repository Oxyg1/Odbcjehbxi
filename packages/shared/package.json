{
  "name": "@tgdonate/shared",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "//exports": "Point at the build, not the sources. tsx and Vite can both transpile .ts on the fly, so exporting src works in dev and during the Vite build — but plain `node dist/main.js` cannot, and that is exactly how the server runs in production. Exporting dist means every consumer resolves the same artefact.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
