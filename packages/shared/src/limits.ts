/**
 * Числовые границы продукта. Всё, что фронт и бэк должны понимать одинаково,
 * лежит здесь — редактор не имеет права выдумывать свои пределы.
 */

/** Пропорции холста: формат Telegram Stories, 1080x1920. */
export const CANVAS_ASPECT = 9 / 16;

/**
 * Защищённые зоны, в долях высоты холста.
 * Сверху — системная плашка Telegram, снизу — кнопка доната и тикер.
 * Слой туда заезжать может, но получает затемняющий scrim.
 */
export const SAFE_ZONE_TOP = 0.12;
export const SAFE_ZONE_BOTTOM = 0.17;

/** Сколько слоёв всего помещается на стенде. */
export const MAX_LAYERS = 40;

/**
 * Бюджет анимации: столько gif/Lottie-слоёв держит средний Android в WebView
 * без просадки. Превышение не запрещено, но требует явного подтверждения.
 */
export const ANIMATION_BUDGET = 5;

/** Ширина слоя в долях ширины холста. 0.03 — мелкий значок, 3 — заливка фона. */
export const SCALE_MIN = 0.03;
export const SCALE_MAX = 3;

/** Ширина нового слоя при добавлении из инвентаря. */
export const SCALE_DEFAULT = 0.42;

/**
 * Центр слоя может уходить за холст, но не дальше, чем на половину его размера,
 * иначе слой теряется и его невозможно взять пальцем.
 */
export const POSITION_MIN = -0.5;
export const POSITION_MAX = 1.5;

/** Шаг притяжения угла поворота, в градусах. */
export const ROTATION_SNAP_STEP = 15;
/** Насколько близко к шагу нужно подойти, чтобы сработало притяжение. */
export const ROTATION_SNAP_THRESHOLD = 4;
