services:
  bot:
    build: .
    container_name: tg-business-watcher
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /app/data/tracker.db
      MEDIA_DIR: /app/data/media
    volumes:
      - ./data:/app/data
